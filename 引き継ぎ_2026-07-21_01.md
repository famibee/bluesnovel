# bluesnovel 新規エンジン開発 引き継ぎ（6回目）

日付: 2026-07-21（続き: `引き継ぎ_2026-07-20_02.md` の次）
対象リポジトリ: `bluesnovel`

## 次回セッションへの引き継ぎ事項（TODO・優先順位順、前回からの持ち越し分含む）

- [ ] グループ位置指定/移動（face合成した画像群を1つの単位として、デザインモードで位置調整・移動する仕様の検討）
  - face合成そのもの（`add_face`/`[lay fn=... face=...]`）は実装・テスト済み（`ScriptEngine.ts`/`store.tsx`/`GrpLayer.tsx`）
  - デザインモードでのMoveableリサイズ時、差分画像（face）は`dx`/`dy`が絶対px指定のため、拡大縮小に追随しない（`GrpLayer.tsx`）。比率換算の要否を検討
  - 本家 `skynovel_esm/src/sn/GrpLayer.ts` のように「`face`のみ指定して直前の`fn`を維持する」独立更新には未対応（現状は`fn`省略時は`face`指定があっても`chgPic`アクション自体を発行しない）
- [ ] 実機（`tmp_blues`）で以下を確認
  - [ ] 読み戻り（PageUp/PageDown）から戻った際、既読部分が瞬時表示されない
  - [ ] `main.sn`へ`[lay layer=mes b_alpha=...]`を仕込んで、文字レイヤ背景の不透明度変更の見た目を確認（現状`main.sn`には未追記）
- [ ] `b_alpha`の値域（0.0〜1.0）を超える値のバリデーション（クランプ）は未実装（`rgba()`側で1超は1として扱われるため実害はないが、明示エラーにすべきか検討）
- [ ] 型チェック（`tsc --noEmit`）がテスト実行フロー（`bun test`）に含まれておらず、`Main.tsx`側の接続漏れ（`chgBAlpha`未接続）が実行時まで気づけなかった実績あり。CI/pre-commit等への追加を検討
- [ ] 動画・音声対応
- [ ] `if/elsif/else/endif` の実装（`skynovel_esm/src/sn/ScriptIterator.ts` の `#if()` 相当を参照）
- [ ] `call`/`return`/マクロ（コールスタック）の実装。`bluesnovel/src/sn/CallStack.ts` は型だけ移植済み。
- [ ] 複数ファイル（`jump fn=...`）対応、`ScriptMng` 側でのファイル切替設計の具体化
  - [button]も現状は同一ファイル内ラベルのみ対応（`jump`と同じ制約）。複数ファイル対応時は
    `jumpToLabel`側も合わせて拡張が必要。
- [ ] `SAMPLE_SN` フォールバック（`ScriptMng.ts`）と `GrpLayer.tsx` の `try/catch` を撤去し、正規のアセットパイプライン（`path.json`）を用意した上で正しいロードエラー処理に戻す
- [ ] 文字送り演出のパラメータ（`duration: 0.25`, `stagger: 0.035`）は仮値。実機（`tmp_blues`）で調整
- [ ] `title.sn`（本家形式）を実際に動かす場合は、対応タグの拡充（`img`/`clear_lay`/`event`/`trans`/`wt`/`bgm`等）が必要。`button`は対応済み。
- [ ] npmリリース処理を `skynovel_esm` に合わせる（後々の対応・未着手）
- [ ] `package.json` から `store`, `socket.io-client` を除去（後々の対応・未着手）
- [ ] skynovel_esm側もGSAP化を検討中（bluesnovelの `@tweenjs/tween.js` は現状未使用のまま残置。撤去はnpmリリース処理整備と合わせて後日）
- [ ] `[button]`の見た目・レイアウト（現状は簡易スタイルのみ。デザインモード時の位置調整対応や、複数ボタン時の並び方など）は未検討
